# frontend-workers-meetup

## Setup

Install the dependencies:

```bash
pnpm install
```

## Run

Start `@malt/nuxt-jobs` development server on http://localhost:3000:

```bash
pnpm --filter @malt/nuxt-jobs dev
```